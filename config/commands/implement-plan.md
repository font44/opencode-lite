---
description: Implement plan
---
Implement plan: $ARGUMENTS

- Once you are done, review using a subagent with `/review_code`, incorporate feedback and (if needed) test again.
- No code comments unless required for functionality.
- `git commit` only once user instructs.
